# Sequential version
import glob
from sequentialLinter import checkFile as run

directory = input("Enter your directory: ")
lstOfFiles = []
lstOfFiles.append()
for i in lstOfFiles:
    error = run(i)
    print(error)
